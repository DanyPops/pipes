# @danypops/pi-pipes

Scaffold only. This will register a `/pipes` command opening a TUI menu
(backend → job → action) as a thin authenticated client to
`@danypops/pipes-daemon` — no network access or credentials of its own.
